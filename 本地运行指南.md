# OpenDevin AI Agent 本地运行指南 (Windows)

这是一个完整的AI Agent平台，可以通过Web界面与AI助手交互，让AI帮你编写代码、分析项目、修复bug等。

## 🎯 OpenDevin 是什么？

OpenDevin是一个自主的AI软件工程师平台，它可以：
- 🤖 通过聊天界面与AI交互
- 💻 自动编写和修改代码
- 🔍 分析和调试项目
- 📝 生成文档和测试
- 🛠️ 执行终端命令
- 📁 管理文件和项目结构

## 🚀 在Windows上运行OpenDevin

### 方法1: 使用Docker (推荐)

这是最简单的方法，OpenDevin官方推荐：

#### 1. 安装Docker Desktop
- 下载并安装 [Docker Desktop for Windows](https://www.docker.com/products/docker-desktop/)
- 启动Docker Desktop

#### 2. 创建工作目录
```powershell
# 创建一个工作目录
mkdir C:\opendevin-workspace
cd C:\opendevin-workspace
```

#### 3. 设置环境变量
```powershell
# 设置你的DeepSeek API密钥
$env:LLM_API_KEY = "your-deepseek-api-key-here"
$env:LLM_MODEL = "deepseek-chat"
$env:LLM_BASE_URL = "https://api.deepseek.com"
```

#### 4. 运行OpenDevin
```powershell
# Windows PowerShell命令
$WORKSPACE_BASE = "$(pwd)/workspace"
docker run -it `
    --pull=always `
    -e LLM_API_KEY=$env:LLM_API_KEY `
    -e LLM_MODEL=$env:LLM_MODEL `
    -e LLM_BASE_URL=$env:LLM_BASE_URL `
    -e WORKSPACE_MOUNT_PATH=$WORKSPACE_BASE `
    -v "${WORKSPACE_BASE}:/opt/workspace_base" `
    -v /var/run/docker.sock:/var/run/docker.sock `
    -p 3000:3000 `
    --add-host host.docker.internal:host-gateway `
    --name opendevin-app-$(Get-Date -Format "yyyyMMddHHmmss") `
    ghcr.io/opendevin/opendevin
```

#### 5. 访问Web界面
打开浏览器访问: http://localhost:3000

### 方法2: 本地开发环境运行

如果你想修改代码或进行开发：

#### 1. 安装依赖
```powershell
# 安装Python 3.11
# 安装Node.js 18+
# 安装Poetry
pip install poetry

# 克隆项目
git clone https://github.com/frank8572888/devin.git
cd devin
```

#### 2. 安装Python依赖
```powershell
poetry install
```

#### 3. 安装前端依赖
```powershell
cd frontend
npm install
cd ..
```

#### 4. 配置API
创建 `config.toml` 文件：
```toml
[llm]
model = "deepseek-chat"
api_key = "your-deepseek-api-key-here"
base_url = "https://api.deepseek.com"

[core]
workspace_base = "./workspace"
```

#### 5. 运行应用
```powershell
# 方法A: 同时启动前后端
make run

# 方法B: 分别启动
# 启动后端
make start-backend
# 启动前端 (新开一个终端)
make start-frontend
```

#### 6. 访问应用
打开浏览器访问: http://localhost:3000

## 🎮 如何使用OpenDevin

### 1. 基本对话
在Web界面的聊天框中输入你的需求：
```
请帮我创建一个Python计算器程序
```

### 2. 项目分析
```
分析当前workspace目录下的代码，找出可以改进的地方
```

### 3. 代码生成
```
创建一个Flask Web应用，包含用户注册和登录功能
```

### 4. Bug修复
```
检查我的Python代码中的错误并修复
```

### 5. 文档生成
```
为这个项目生成详细的README文档
```

## 🛠️ 高级配置

### 自定义Agent
在Web界面中可以选择不同的Agent类型：
- **CodeActAgent**: 通用编程助手
- **PlannerAgent**: 项目规划专家
- **BrowsingAgent**: 网页浏览和搜索

### 调试模式
```powershell
# 启用调试模式
$env:DEBUG = "1"
make start-backend
```
调试日志会保存在 `logs/llm/` 目录下。

### 工作目录
- 所有AI生成的文件都会保存在 `workspace` 目录
- 你可以在这个目录放入现有项目让AI分析

## 🔧 常见问题

### 1. Docker相关问题
```powershell
# 检查Docker是否运行
docker --version

# 如果遇到权限问题，确保Docker Desktop已启动
```

### 2. 端口占用
```powershell
# 如果3000端口被占用，可以换个端口
docker run ... -p 3001:3000 ...
# 然后访问 http://localhost:3001
```

### 3. API密钥问题
- 确保DeepSeek API密钥正确
- 检查网络连接是否正常
- 确认API配额是否充足

### 4. 内存不足
```powershell
# 如果遇到内存问题，可以限制Docker内存使用
docker run ... --memory=4g ...
```

## 💡 使用技巧

### 1. 明确的指令
❌ 不好的指令: "写个程序"
✅ 好的指令: "创建一个Python程序，实现文件批量重命名功能，支持正则表达式"

### 2. 分步骤执行
对于复杂任务，可以分步骤进行：
```
第一步：创建项目结构
第二步：实现核心功能
第三步：添加测试用例
第四步：编写文档
```

### 3. 利用上下文
OpenDevin会记住对话历史，可以基于之前的代码继续开发：
```
在刚才的计算器基础上，添加科学计算功能
```

### 4. 代码审查
```
请审查我刚写的代码，检查是否有安全问题和性能优化空间
```

## 📁 项目结构

```
devin/
├── opendevin/          # 后端核心代码
│   ├── core/          # 核心功能
│   ├── controller/    # Agent控制器
│   ├── llm/          # LLM接口
│   └── server/       # Web服务器
├── frontend/          # 前端界面
├── agenthub/         # 各种Agent实现
├── workspace/        # AI工作目录
└── config.toml       # 配置文件
```

## 🎉 开始使用

1. 选择上面的任一方法启动OpenDevin
2. 在浏览器中打开 http://localhost:3000
3. 在聊天界面输入你的第一个任务
4. 观看AI为你编写代码！

现在你就可以拥有一个24小时在线的AI程序员助手了！🚀